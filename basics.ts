let user: string = "Amine";
let hasLoggedIn: boolean = true;
let num :number = 10;

let regexVariable: RegExp = /bar/;

const users: string[] = user.split(" ");

hasLoggedIn += " a string";

console.log(hasLoggedIn);